# Zaim Budget PWA

Zaim CSVをブラウザ内で読み込み、今月の貯蓄率と予算消化ペースをスマホで確認する静的PWAです。

CSVデータは外部送信せず、端末内のIndexedDBに保存します。

## Commands

```bash
npm install
npm run dev
npm run test
npm run build
```

GitHub Pages向けに `base: "./"` でビルドします。
